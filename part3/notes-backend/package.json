{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../../FullStackOpenExercise/part2/notes-frontend && npm run build && @powershell Copy-Item dist -Recurse ../../../FSOpart3/notes-backend/dist",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "lint": "eslint ."
  },
  "author": "Caedan Wang",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.5.0",
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.5",
    "@stylistic/eslint-plugin-js": "^1.7.2",
    "eslint": "^8.5.0",
    "eslint-plugin-react": "^7.34.1",
    "globals": "^15.0.0",
    "nodemon": "^3.1.0"
  }
}
